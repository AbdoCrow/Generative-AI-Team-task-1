here I will write my thought process
so in the first phase it's not an llm yet but it still need to answer the user according to a set of questions so let's  disucss what files I will need 
- first I will need an interface UI for the user to interact with I don't know streamlit so this will be fun
- I need a parser to know what the user need without an LLM 
- lastly I will need a data acess layer with all of the functions the AI can use to get what the user want

let's start by the main file which will be the data access layer